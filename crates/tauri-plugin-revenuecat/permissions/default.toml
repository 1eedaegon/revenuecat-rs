"$schema" = "schemas/schema.json"

[default]
description = "Allows the app to query products, purchase, restore, and finish transactions through the platform store."
permissions = [
    "allow-query-products",
    "allow-purchase",
    "allow-query-purchases",
    "allow-finish-transaction",
]
