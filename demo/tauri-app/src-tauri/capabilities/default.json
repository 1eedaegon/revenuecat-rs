{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Let the webview drive RevenueCat through the plugin.",
  "windows": ["main"],
  "permissions": ["core:default", "revenuecat:default"]
}
